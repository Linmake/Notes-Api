[build]
  publish = "dist"  # 👈 Agrega esta línea
  command = "npm run build"  # 👈 Y esta

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Opcional: Para mejor performance
[[headers]]
  for = "/*"
  [headers.values]
    "Cache-Control" = "public, max-age=0, must-revalidate"